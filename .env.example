# Showcase build — IngatlanCheck runs fully self-contained, no external services required.
# Optional: override the public app URL (used only for absolute metadata/OG).
NEXT_PUBLIC_APP_URL=http://localhost:3000
